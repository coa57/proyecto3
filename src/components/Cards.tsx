import { Link, useNavigate } from "react-router-dom";
import type { Room } from "../types/hospitality";
export const Stars = ({ value }: { value: number }) => <span className="stars">{"★".repeat(value)}{"☆".repeat(5 - value)}</span>;
export function RoomCard({ room }: { room: Room }) { const navigate = useNavigate(); const available = room.status === "Disponible"; return <article className="room-card"><img src={room.image} alt={`${room.type} ${room.number}`} /><div className="card-body"><div className="card-top"><span className={available ? "badge available" : "badge"}>{room.status}</span><b>Hab. {room.number}</b></div><h3>{room.type}</h3><p>⌂ Piso {room.floor} · ♙ {room.capacity} {room.capacity === 1 ? "persona" : "personas"}</p><p className="amenities">{room.amenities.slice(0, 3).join(" · ")}</p><div className="card-bottom"><strong>Bs {room.price}<small> / noche</small></strong><div><Link className="text-link" to={`/rooms/${room.id}`}>Detalles</Link>{available && <button className="button small" onClick={() => navigate(`/reservation?room=${room.id}`)}>Reservar</button>}</div></div></div></article>; }
